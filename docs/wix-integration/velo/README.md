# Moved

The Velo code that used to live here was relocated to a clean, independent Velo project at
**`/wix-site`** following the `/backend` `/pages` `/public` structure.

➡️ Code: `../../wix-site/backend/…`
➡️ This `wix-integration/` folder now holds **design + setup docs only** (`INDEX.md`, `SETUP.md`).

(This empty `velo/` shell could not be auto-deleted from the synced folder — safe to remove manually.)
